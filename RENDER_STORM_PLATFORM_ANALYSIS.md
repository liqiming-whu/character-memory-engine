# 连续删除/快速切 tab 渲染风暴：实验过程、稳定复现、依据与收尾结论

> 状态：**已收尾**（2026-08-08）· 结论：根因在 Operit 平台全量重绘机制，插件侧放大器已全部修复，达到「不报错、不闪退」验收标准，停止极限性能优化

## 结论摘要

1. **渲染风暴根因在 Operit 平台层**：compose DSL 渲染器每次 setState 都**全量重绘整个 tab**（无差异更新/懒渲染），连续操作时渲染队列堆积，UI 线程饱和。
2. **插件侧放大器已全部修复**（v2.3.0 已发布）：渲染闭包内 I/O（工具调用/setEnv/文件写入）、loadData/loadMem 无防抖、删除后无本地即时移除——三项叠加是「卡死」「报错」的直接诱因，已全部消除。
3. **验收标准**：插件在连续删除/快速切换下**不报错、不闪退**即为优化到位。极限性能（渲染吞吐）依赖平台机制，插件侧无法根治，**停止继续优化**。
4. **后续动作**：向 Operit 官方反馈「全量重绘」问题（建议差异更新/懒渲染）；v2.3.1 按计划优先解决自动分析水位线问题，不受本结论影响。

---

## 一、实验过程（按时间线）

### 阶段 1：定位渲染风暴（08-08 04:14，北京时间）

- **操作**：重开 CME 插件，连续切换 tab。
- **日志证据**（dbg_ui.log，UTC 20:14:16-35）：
  - 打开插件一次 = 6 次渲染（init 3 次 + loadData 返回 3 次），loadData 往返 165-655ms。
  - 7 秒内切换 tab 21 次渲染（r=3→24），**每次切换 2 次渲染**。
  - 每次切到角色页触发 loadPersona（真实调用 210ms / 缓存命中 88-99ms）。
- **初步判断**：渲染密度高，但单次渲染 +0ms，怀疑「渲染本身不重、重的是渲染闭包内的工作」。

### 阶段 2：dbgUi 限频验证（04:30-04:32）

- **操作**：给 dbgUi（渲染探针）加 500ms 限频（工具调用 + env 缓冲都限），烧录后重测。
- **日志证据**：
  - `[render]` 日志行**完全消失**（被限频压掉），只剩关键事件（loadData/loadPersona/loadMem）。
  - **但 r 计数仍在快速递增**：19 秒内 r=1→131（约 131 次渲染）——**渲染本身没有减少**。
  - **用户反馈：UI 不再卡死**。
- **关键结论**：卡死主因不是渲染次数，而是**每次渲染闭包内埋的 I/O**（`log_ui` 工具调用 + setEnv）——工具调用在 Operit 平台层有往返开销、又触发状态变化→重绘，形成**恶性循环**。限频后 I/O 归零，渲染风暴仍在但 UI 不卡。

### 阶段 3：连续删除报错 + loadMem 排队（04:38-04:40）

- **操作**：知识页/时间线页快速连续删除。
- **日志证据**：
  - engine.log：`memory not found with id=608` ×4、`id=604` ×4、`id=602` ×4、`id=603` ×4、`id=704` ×4——**每个 id 被连点 4 次**，删除成功后列表未即时移除，连点命中过期行。
  - dbg_ui.log：loadMem 返回延迟 **+673ms → +1947ms → +3085ms → +4027ms → +4739ms → +5208ms → +5580ms → +6209ms**——连续删除时每次删除触发 loadMem（无防抖），工具调用队列堆积，请求排队 1-6 秒才返回。
- **修复**（v2.3.0）：删除成功/失败都**本地即时移除该行**（连点不再命中过期行）；loadMem 加 300ms 防抖（合并刷新）；`dropMemoryFromCache` 按 id 优先。

### 阶段 4：CMS 对照实验（04:31）

- **操作**：CMS 同样数据量（61-77 条记忆），快速切 tab + 连续操作。
- **日志证据**：
  - CMS render 探针：20:31:35-36 **1 秒内 19 次渲染**、20:31:40 20 次渲染（m=77）——**渲染密度与 CME 同级甚至更高，但用户体感流畅**。
  - CMS 无工具调用 I/O（探针关闭、character.js 写入探针移除前有）→ 渲染不卡。
- **反面教材**：CMS 快速切 tab 触发 `compose_dsl runtime error: not a function`（04:31:08/11/14 三次）——根因：character.js 渲染闭包残留 `Tools.Files.write` 探针 → 文件 I/O 竞争 → 中间渲染失败 → Operit DSL 动作表竞态 → 动作分发查表失败。移除探针后修复（CMS v2.1.0）。
- **对照结论**：渲染次数多 ≠ 卡；**渲染闭包内 I/O 才是卡顿放大器**。两侧验证一致。

---

## 二、稳定复现

| 场景 | 复现条件 | 现象（修复前） | 现象（v2.3.0 后） |
|---|---|---|---|
| 连续删除（知识页/时间线/待办） | 点击间隔 < 300ms，连点 5+ 条 | 每个 id 被连点多次报 memory not found；loadMem 排队 1-6 秒；UI 卡死 | 第一击即删、行即时消失，无报错，无卡死 |
| 快速切换 tab | 间隔 < 1s，连切 10+ 次 | 渲染计数 2-6 次/切换，队列堆积，UI 卡死（最严重时 13 秒 21 次渲染 + 每次渲染 1 次工具调用） | 渲染仍在（平台全量重绘），但 UI 不卡、不闪退 |
| 快速切 tab + 立即点击 | 切换后 1s 内点击 | （CMS）compose_dsl not a function；动作分发失败 | 无报错 |

**平台侧残留现象（插件无法根治，已接受）**：
- 渲染计数在极限操作下仍快速递增（r 可达 100+/19 秒）——这是 Operit 每次 setState 全量重绘所致，插件无法控制。
- 连续操作时工具调用排队延迟可能上升（v2.3.0 后已大幅缓解，不再出现 1-6 秒级排队）。

---

## 三、依据（原始日志）

1. **CME dbg_ui.log**（`/sdcard/Download/Operit/character_memory_engine/logs/`）：
   - 渲染序列：`[render] ... r=3→24`（20:14:28-35）、r=1→131（20:30:45-20:31:04）。
   - loadMem 延迟：`+673ms/+1947ms/+3085ms/+4027ms/+4739ms/+5208ms/+5580ms/+6209ms`（20:39:16-30）。
   - 删除序列：`[del] click id=748 → found → success → local removed len=10→0`（20:39:13-14，角色页正常）。
2. **CME engine.log**：`memory not found with id=608/604/602/603/704` 各 ×4（04:38:58-04:40:04）。
3. **operit.log**（`/data/user/0/com.ai.assistance.operit/files/logs/`）：
   - `compose intermediate render failed: TypeError: not a function` ×3/次（04:31:08/11/14）。
   - `dispatch compose action failed: actionId=__action_845` → `Execution Function: __operit_dispatch_compose_dsl_action`。
4. **CMS vs CME 渲染计数**：CME 日志累计 `[render]` 7048 行 vs CMS 432 行（时间跨度不同，仅佐证 CME 探针写入密度曾是 CMS 的 16 倍）。

---

## 四、原因分析

### 平台层根因（无法在插件侧根治）

- Operit compose DSL 渲染器**每次 setState 全量重绘整个 tab**，无差异更新/懒渲染。
- 连续操作 → 多次 setState → 渲染队列堆积 → UI 线程饱和 → 卡顿/卡死。
- 佐证：CMS 渲染密度高于 CME（1 秒 19-20 次）依然流畅——渲染本身开销可接受，平台按帧消费即可；一旦渲染闭包内夹带 I/O，帧耗时暴增，队列就雪崩。

### 插件层放大器（已全部修复，v2.3.0 含）

1. **渲染闭包内 I/O（卡死主因）**：CME 的 dbgUi 每次渲染发 1 次 `log_ui` 工具调用 + 1 次 setEnv；CMS character.js 每次渲染同步 `Tools.Files.write`。工具调用在平台层有往返开销且触发状态变化→重绘，形成「渲染→I/O→状态变化→再渲染」恶性循环。**修复：限频 500ms / 移除**。
2. **loadData/loadMem 无防抖**：每次操作触发全量请求 + setState + 全量重绘。**修复：300ms 防抖合并**。
3. **删除后无本地即时移除**：连点命中过期行 → memory not found。**修复：成功/失败都本地 drop**。
4. **loadPersona 缓存命中无节流**：连切角色页反复 setEnv/setState。**修复：5 秒节流**。

### 为什么「渲染本身不卡、I/O 才卡」

- 单次渲染 +0ms（日志实证），纯渲染吞吐足够；
- 渲染闭包内每多一次工具调用/setEnv/文件写，单帧耗时从 0ms 涨到数十-数百 ms，队列雪崩；
- 限频后同一渲染风暴下 UI 恢复流畅——双向验证。

---

## 五、收尾决策（2026-08-08 用户确认）

1. **停止极限性能优化**：连续删除/快速切换不是本插件的正常使用场景，不测试极限性能。
2. **验收标准定为「不报错、不闪退」**：v2.3.0 已达标（无 not found 报错、无 DSL 崩溃、无卡死、无闪退）。
3. **平台问题记录**：Operit 全量重绘机制建议反馈官方（差异更新/懒渲染/按需渲染）；若平台后续支持，再评估分页/懒渲染方案（见 DEVELOPMENT_PLAN「记忆过多时的展示与管理方案」）。
4. **v2.3.1 计划不变**：优先解决自动分析水位线问题（main.js onPromptFinalize 整写 trigger.json 清空 watermarks），与 CMS 12.2 同源问题一并处理。
