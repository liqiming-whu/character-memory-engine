锁机制采用 `mkdir /tmp/cme_start_worker.lock` 的原子创建实现 single-flight，创建失败的并发实例立即退出。
锁从脚本进入关键流程开始持有，退出时通过 `trap 'rm -rf $LOCK_DIR' EXIT` 清理；正常路径下可阻止双启动。
风险是 `SIGKILL` 等无法执行 trap 的终止会遗留锁目录，导致后续启动被误判为“已有实例运行”。
