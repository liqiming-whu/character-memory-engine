# CME 高级记忆能力优化计划

## 背景

CME 当前已经实现：

- 对话历史分析；
- AI 自动提取记忆；
- 记忆数据库存储；
- 语义检索；
- 自动将相关记忆注入提示词。

当前主要问题：

1. 自动提取质量不稳定；
2. 长会话分析容易出现超时、截断或质量下降；
3. 技术开发过程容易污染长期记忆；
4. 缺少 Agent 主动维护记忆和工作区状态的稳定机制；
5. 分析水位线周期性丢失（main.js onPromptFinalize 整写 trigger.json 清空 watermarks），导致每次打开插件都触发全量分析（30-40s）——根因已实证，排期 v2.3.1 优先解决（详见 4.2）。

本阶段目标是将 CME 从“自动总结对话内容”升级为：

> 自动识别真正值得长期保存的信息，并逐步支持 Agent 持续维护自身上下文。

---

# 一、自更新系统

> 高级功能，降低优先级。

## 1.1 目标

实现 Agent 根据长期互动自动维护：

- CME Memory DB；
- 工作区文件；
- 角色状态；
- 用户模型；
- 与当前角色绑定的长期上下文。

理想流程：

```text
对话
  ↓
判断是否产生长期变化
  ↓
提取候选信息
  ↓
决定是否写入
  ↓
更新 CME DB 或工作区文件
  ↓
未来对话自动使用
```

---

## 1.2 自动判断并写入 CME DB

### 当前状态

CME 已实现：

- API 分析对话历史；
- 自动提取记忆；
- 写入 CME 数据库；
- 自动检索并注入记忆。

尚未稳定实现：

- Agent 根据当前对话主动判断是否需要保存记忆；
- 角色主动调用 CME 写入能力；
- 不依赖角色提示词主动性的稳定写入机制；
- 将官方记忆库的自动保存流程替换为 CME DB。

### 主要问题

目前角色有时能够根据对话内容主动更新场景 JSON 文件，但触发：

- 不稳定；
- 时机难以预测；
- 频率极低；
- 较依赖角色提示词、Skill 是否被加载以及模型是否主动意识到需要写入。

因此，不能仅依赖“在提示词中要求角色记得更新”。

### 研究方向

研究是否可以增加独立的记忆评估流程：

```text
新对话或新增消息
  ↓
Memory Evaluator
  ↓
判断是否出现长期有效信息
  ↓
生成结构化候选记忆
  ↓
调用 CME 写入接口
```

该流程应尽量与角色正常回复解耦，避免依赖角色在生成回复时主动想起记忆工具。

### 需要确认的问题

- 官方自动保存记忆是否由独立评估器触发；
- 官方自动保存和手动保存是否使用不同提示词或不同处理链路；
- 官方是否在对话结束后额外运行一次记忆判断；
- 官方是否只向模型暴露工具，还是存在系统层自动调用；
- 官方自动更新 `user.md` 的触发条件、调用方式和失败处理；
- 是否可以参考官方实现，为 CME 增加类似的后台记忆评估节点。

### CME 目标方案

保存：

- 用户长期偏好；
- 用户长期习惯；
- 明确的生活事件；
- 身份、关系或长期目标变化；
- 角色与用户的共同经历；
- 长期有效的项目架构决策；
- 会影响未来 Agent 行为的规则和约定。

默认不保存：

- 单次 Bug 修复；
- 临时调试过程；
- 命令输出；
- 错误日志；
- 工具调用细节；
- 临时测试结果；
- 已完成的一次性操作；
- 仅对当前任务有用的信息。

---

## 1.3 自动根据对话内容召回 CME 记忆

> 高级功能，优先级较低。

### 当前状态

CME 已实现自动根据用户输入进行语义检索，并将相关记忆注入提示词。

因此，基础的“自动召回并注入”能力已经存在。

### 后续可研究方向

进一步研究 Agent 主动召回能力：

```text
当前问题
  ↓
Agent 判断现有上下文不足
  ↓
主动查询 CME
  ↓
获取更精确或更深层的记忆
  ↓
结合记忆继续回答
```

可考虑提供：

- `recall_cme_memory`
- `search_cme_memory`
- `get_memory_detail`

等工具，让 Agent 在自动注入结果不足时进行二次检索。

### 优先级说明

由于自动语义检索和提示词注入已经实现，该能力暂不属于近期重点。

---

## 1.4 自动更新工作区对应文件

> 高级功能，降低优先级。

### 当前状态

工作区中已经包含：

- 用户个人信息；
- 场景状态；
- 角色状态；
- 项目资料；
- 自定义长期信息。

角色可以通过文件工具自由编辑工作区文件。

过去主要依赖 Skill 控制：

- 何时读取；
- 何时更新；
- 更新哪些文件；
- 文件格式要求。

但 Skill 采用按需加载，导致更新触发不稳定。

### 目标

实现 Agent 根据对话内容稳定更新：

- `scenario.json`；
- 角色状态文件；
- 用户状态文件；
- 项目长期配置；
- 其他由工作区维护的结构化数据。

### 可能方案

#### 方案 A：继续依赖角色主动调用文件工具

优点：

- 实现简单；
- 不需要新增系统组件。

缺点：

- 高度依赖提示词；
- 触发不稳定；
- 容易漏更新；
- 不适合关键状态维护。

#### 方案 B：独立状态评估器

```text
对话结束
  ↓
State Evaluator
  ↓
判断是否发生状态变化
  ↓
生成结构化 Patch
  ↓
更新对应文件
```

该方案比让角色自由重写整个文件更稳定。

#### 方案 C：结构化更新工具

参考官方 `update_user_md` 的思路，不允许模型任意重写文件，而是提供：

- 固定字段；
- Patch 更新；
- 类型校验；
- 允许更新的文件白名单；
- 修改历史；
- 回滚能力。

### 参考方向

必要时研究官方自动更新 `user.md` 的实现，包括：

- 是否由角色主动调用；
- 是否由后台流程判断；
- 是否依赖工具描述；
- 是否存在字段级更新；
- 是否保存更新日志；
- 是否有失败重试或冲突检测。

---

## 1.5 Skill 自更新

> 暂不实现。

原因：

- 依赖官方提供更新 Skill 的工具；
- 当前缺少安全、稳定的 Skill 修改接口；
- 允许 Agent 自行修改 Skill 风险较高；
- 在记忆准入和状态更新机制成熟之前，收益有限。

后续只有在官方提供可靠的 Skill 更新工具后，再评估：

- 自动修改 Skill 内容；
- 根据长期使用结果优化 Skill；
- 保留版本和回滚；
- 修改前后自动测试；
- 限制可修改范围。

---

# 二、优化分析对话记忆提取

> 高级功能，但相较自更新系统优先级更高。

## 2.1 当前实现

当前提取提示词：

```python
EXTRACTION_PROMPT_TEMPLATE = """你是一个记忆系统。请理解以下对话整体讲了什么，然后提取有价值的信息。{persona_hint}

核心原则：
- 你是在理解一段对话后做总结，不是逐条扫描消息
- 一段对话可能只产生0-2条有价值的提取，这是正常的
- 过程噪音（反复调试、重复提问、工具调用细节）不要提取
- 无效信息（"继续""好的""开始"等）完全忽略
- 如果与已有数据语义重复，不要重复提取；同一事件措辞不同但语义相同也只保留一条
- 不推断未明确表达的人格、感情或关系等级
{existing_summary}
返回纯JSON（不要markdown代码块，不要任何额外文字）：
{"events":[{"type":"activity|schedule|observation|milestone|mood","title":"标题","description":"描述","importance":"high|medium|low","date":"YYYY-MM-DD","time":"HH:MM"}],"todos":[{"title":"待办事项","description":"描述","priority":"high|medium|low","dueDate":"YYYY-MM-DD或null","completed":false}],"contacts":[{"name":"姓名","relation":"friend|family|colleague|classmate|service|other","attributes":[{"key":"属性名","value":"值"}],"context":"提到这个人的场景"}],"info":[{"category":"类别","content":"内容"}],"finance":[{"type":"expense|income","category":"类别","amount":0,"description":"描述","date":"YYYY-MM-DD"}],"menstrual":[{"startDate":"YYYY-MM-DD","endDate":"YYYY-MM-DD或null","symptoms":"症状描述"}],"character":[{"title":"标题","content":"角色身份或背景事实"}],"relationship":[{"title":"标题","content":"用户与角色的明确关系事实或共同经历"}],"preference":[{"title":"标题","content":"用户或角色明确表达的偏好"}],"interaction_rule":[{"title":"标题","content":"明确约定的称呼、回复风格或互动边界"}]}

提取规则：
1. events：有记录价值的事件。activity=做了什么事；schedule=有时间安排的事；observation=发现的现象；milestone=阶段性变化；mood=情绪
2. todos：用户明确要做的事，不是已经做完的事
3. contacts：提到的人物及其属性
4. info：值得记住的知识/事实/参数
5. finance：涉及花钱或收钱的记录
6. menstrual：经期记录
7. character/relationship/preference/interaction_rule：仅在存在当前角色卡且事实明确时提取
8. 某类没数据用空数组；同一件事不要拆成多条
"""
```

当前提示词已经具备以下正确方向：

- 强调整体理解；
- 不逐条机械扫描；
- 允许只提取 0～2 条；
- 过滤调试和工具调用噪音；
- 忽略无效回复；
- 避免重复；
- 限制人格、感情和关系推断；
- 支持空数组。

但当前问题不能只靠继续增加提示词解决，还需要优化分析链路。

---

## 2.2 当前主要问题

### 长会话稳定性不足

表现：

- 会话长时可能超时；
- 截断导致早期关键信息丢失；
- 输入规模过大后，模型判断质量下降；
- 真正重要的信息被大量普通内容掩盖；
- 输出可能不完整或 JSON 结构异常。

已尝试：

- 截断窗口：`6000 → 10000`
- `max_tokens`：`8192 → 16384`

但仍然不稳定，说明问题不只是上下文和输出长度不足。

### 技术噪音过多

当前处于 CME 初期开发阶段，与 AI 的大量对话内容包括：

- Bug 修复；
- 代码修改；
- 测试失败；
- 日志分析；
- 打包过程；
- 参数调整；
- 临时实现细节。

这些内容文本量大、信息密度高，模型容易误认为它们很重要。

结果可能是：

- “今天修了什么 Bug”被写入长期记忆；
- 临时开发步骤占据大量条目；
- 真正的生活事件、关系变化和长期偏好被掩盖。

### 记忆准入门槛不清晰

需要进一步明确：

- 提取不是总结；
- 对话很长不代表必须提取；
- 技术细节很多不代表具有长期价值；
- 一次分析返回全部空数组是正常且高质量的结果；
- 不能为了满足数量要求强行生成记忆。

---

# 三、优化目标

将提取目标从：

> 总结这段对话中发生了什么。

调整为：

> 判断对话中是否存在会在未来持续影响 Agent 行为、理解或决策的信息；如果没有，则不提取。

核心原则：

1. 允许零结果；
2. 宁可漏掉低置信信息，也不要污染长期记忆；
3. 不按对话篇幅决定记忆数量；
4. 不按技术细节丰富程度决定重要性；
5. 区分长期事实和临时上下文；
6. 生活事件不能被技术内容掩盖；
7. 自动提取是兜底手段，不要求覆盖所有可记忆内容；
8. 手动保存可承担自动提取遗漏的重要信息。

---

# 四、长会话分析链路优化

## 4.1 先复现并分类问题

在修改前建立可重复测试样本，分别确认：

- 输入被截断；
- API 请求超时；
- 模型输出超时；
- 输出 token 不足；
- JSON 输出不完整；
- 长输入导致判断质量下降；
- 关键信息在截断区域；
- 技术噪音掩盖生活信息；
- 去重或入库阶段错误。

建议记录：

- 原始消息数；
- 输入字符数；
- 估算 token 数；
- 实际发送范围；
- 截断位置；
- API 请求耗时；
- 模型返回长度；
- JSON 解析结果；
- 候选记忆数量；
- 最终入库数量；
- 被过滤原因。

---

## 4.2 增量分析

当前若重复分析完整历史，会产生：

- 输入越来越长；
- 重复成本增加；
- 旧内容被反复提取；
- 更容易超时；
- 去重压力增大。

建议维护分析游标：

```json
{
  "lastProcessedMessageId": "message-id",
  "lastProcessedAt": "2026-08-07T20:00:00+08:00"
}
```

下一次主要分析：

- 上次成功处理之后的新消息；
- 必要的少量前置上下文；
- 现有记忆摘要；
- 当前角色信息。

不要每次重新发送完整历史。

### 水位线丢失（v2.3.1 优先，根因已实证）

增量分析依赖水位线（watermarks）记录已处理位置，但实测存在周期性丢失：

- **根因**：main.js `onPromptFinalize` 每次消息发送时整写 trigger.json（仅 5 字段：chatId/cooldownStart/callerCardId/personaName），把 `trigger_analysis` 写入的 `watermarks`/`lastAnalyzedAt` 清空 → 水位线丢失 → 打开插件时永远全量分析。
- **佐证**：engine.log 02:03:02/47/55 三次 analyze_chat 成功（32-38s 全量量级）；dbg_call.log 02:03:55 正常返回 no_new_content（当时水位线存在）；此后 trigger.json 被 onPromptFinalize 覆盖，watermarks 消失。
- **修复方向**：main.js 写 trigger.json 前先读旧值，合并保留 `watermarks`/`lastAnalyzedAt` 再写；验证 `MEMORY_SYSTEM_TRIGGER_RESULT` env 在 CME 沙盒可用性；与 CMS 12.2（分析完成但 UI 不刷新）同源问题一并修。
- **排期**：v2.3.1 首个任务（2026-08-08 用户确认，见 DEVELOPMENT_PLAN）。

---

## 4.3 分段或滑动窗口分析

对超长会话采用：

```text
长会话
  ↓
按消息边界切片
  ↓
每段独立提取候选记忆
  ↓
跨段合并
  ↓
统一价值判断
  ↓
去重后入库
```

建议：

- 优先按完整消息切分，不在一条消息中间截断；
- 保留少量重叠上下文；
- 每段允许返回空结果；
- 不要求每段提取记忆；
- 分段结果必须经过第二阶段合并和过滤；
- 避免同一事件跨窗口重复生成。

---

## 4.4 滑动窗口设计原则

可配置：

- 每个窗口最大 token；
- 前后窗口重叠消息数；
- 最大窗口数量；
- 单次分析超时；
- 分段失败重试次数；
- 合并阶段最大候选数。

不建议简单把窗口无限增大，因为更大的上下文不一定带来更好的长期记忆判断。

---

# 五、记忆准入控制

## 5.1 允许无记忆结果

必须明确支持：

```json
{
  "events": [],
  "todos": [],
  "contacts": [],
  "info": [],
  "finance": [],
  "menstrual": [],
  "character": [],
  "relationship": [],
  "preference": [],
  "interaction_rule": []
}
```

这表示：

- 分析成功；
- 当前对话没有值得长期保存的信息；
- 不属于提取失败。

如果需要增强可观测性，可以在内部结果中增加：

```json
{
  "hasMemory": false,
  "reason": "本次内容主要为临时技术调试，没有长期有效信息"
}
```

但是否修改正式输出结构，需要结合现有解析逻辑评估。

---

## 5.2 不设置最低提取数量

禁止：

- 每次至少提取 1 条；
- 长会话必须提取多条；
- 每个类别必须有结果；
- 为避免空结果而降低标准；
- 将对话摘要伪装成记忆。

正确预期：

- 纯 Bug 调试会话：0 条；
- 长时间技术排错：0 条；
- 重复讨论已有结论：0 条或更新已有条目；
- 夹杂一个明确生活事件：只提取该生活事件；
- 明确长期架构决策：提取 1 条项目长期信息。

---

## 5.3 长期价值判断标准

候选信息至少满足一项：

- 在未来数周或数月仍可能有效；
- 会影响 Agent 对用户或角色的理解；
- 会影响未来回复方式；
- 会影响未来决策或工具使用；
- 是明确的长期偏好；
- 是稳定身份、关系或背景事实；
- 是长期目标或计划；
- 是重要生活事件；
- 是明确的共同经历；
- 是长期有效的项目设计决策；
- 用户明确要求保存。

默认拒绝：

- 仅对当前任务有效；
- 单次操作；
- 短期故障；
- 调试细节；
- 日志输出；
- 已解决的小问题；
- 模型或工具临时表现；
- 没有明确证据的推断；
- 仅因为被反复讨论而显得重要的信息。

---

# 六、技术噪音过滤

## 6.1 默认作为临时上下文

以下内容默认不进入长期记忆：

- 修复某个 Bug；
- 某次报错；
- 某条日志；
- 某个命令；
- 某次测试结果；
- 修改某个字段；
- 重新打包；
- 安装依赖；
- 临时参数调整；
- 一次性排错过程；
- 工具调用过程；
- AI 的中间尝试。

## 6.2 可以进入长期记忆的技术内容

技术内容只有在满足以下情况时才提取：

1. 用户明确要求记住；
2. 属于长期架构决策；
3. 是稳定工作方式或工具偏好；
4. 是未来会反复使用的项目事实；
5. 会影响后续 Agent 行为；
6. 是明确的设计原则；
7. 代表项目方向发生变化。

示例：

不提取：

```text
今天修复了 analyze_chat 超时 Bug。
```

可以提取：

```text
CME 后续采用增量分析和分段窗口处理长会话。
```

不提取：

```text
将 max_tokens 从 8192 改成 16384。
```

可以提取：

```text
单纯扩大上下文和 max_tokens 无法解决 CME 长会话提取不稳定问题。
```

是否保存后一条，仍应结合其未来价值和现有项目文档是否已记录来判断。

---

# 七、保护生活事件和长期用户信息

## 7.1 问题

技术对话通常：

- 数量更多；
- 描述更具体；
- 文本更长；
- 有大量实体和操作。

模型容易优先总结技术内容，而忽略只出现一次的生活事件。

## 7.2 优化方向

提取时按类别独立检查，而不是让所有内容竞争同一输出空间。

建议优先检查：

1. 生活事件；
2. 人际关系；
3. 长期偏好；
4. 身份变化；
5. 长期目标；
6. 健康或习惯；
7. 共同经历；
8. 互动规则；
9. 项目长期决策；
10. 其他长期信息。

临时技术过程最后检查，并默认排除。

---

# 八、提取流程优化

## 8.1 当前流程

```text
对话
  ↓
一次性直接生成最终记忆
  ↓
去重
  ↓
入库
```

## 8.2 推荐流程

```text
新增对话
  ↓
分段或增量读取
  ↓
提取原子候选信息
  ↓
长期价值判断
  ↓
类别归档
  ↓
跨窗口合并
  ↓
与现有记忆比对
  ↓
store / merge / update / supersede / discard
  ↓
写入 CME DB
```

## 8.3 分两阶段调用

如成本允许，可以拆成：

### 第一阶段：候选发现

只负责找出明确事实，不决定最终入库。

### 第二阶段：准入判断

判断：

- 是否长期有效；
- 是否重复；
- 是否为临时噪音；
- 是否需要合并或覆盖；
- 是否应该丢弃。

优点：

- 更容易定位错误；
- 可以区分“没发现”和“发现后拒绝”；
- 可单独优化每个阶段。

缺点：

- API 成本增加；
- 分析耗时增加；
- 实现复杂度提高。

因此可先通过单次结构化输出模拟两阶段，必要时再拆成两个 API 调用。

---

# 九、分类与生命周期

## 9.1 用户长期记忆

包括：

- 偏好；
- 习惯；
- 身份；
- 长期目标；
- 生活背景；
- 健康相关长期事实；
- 人际关系；
- 重要生活事件。

## 9.2 角色长期记忆

包括：

- 角色身份；
- 角色背景；
- 用户与角色的明确关系；
- 双方共同经历；
- 长期互动规则；
- 明确约定的称呼、语气和边界。

## 9.3 项目长期记忆

包括：

- 架构决策；
- 长期技术方向；
- 设计原则；
- 稳定工作流程；
- 长期有效配置；
- 用户对项目的稳定要求。

## 9.4 临时工作上下文

包括：

- Bug；
- 调试；
- 日志；
- 测试；
- 一次性命令；
- 当前任务步骤；
- 临时参数；
- 尚未确认的尝试。

默认不进入长期记忆库。

如未来有需求，可单独进入：

- 项目日志；
- 会话摘要；
- 临时状态库；
- 短期记忆缓存。

不应与长期用户记忆混合。

---

# 十、提取提示词优化建议

当前提示词可继续作为基础版本，但建议强化以下内容。

## 10.1 增加“允许完全无结果”

建议加入：

```text
你的任务不是保证产生记忆，而是判断是否存在值得长期保存的信息。

即使对话很长，也可能没有任何值得保存的内容。

如果没有符合长期记忆标准的信息，所有类别返回空数组。

禁止为了避免空结果而提取低价值内容。
```

## 10.2 增加长期价值标准

建议加入：

```text
只有未来仍可能影响 Agent 对用户、角色、关系、项目或行为方式的理解时，才提取。

仅对当前任务有用、很快过期、已经完成的一次性操作，不属于长期记忆。
```

## 10.3 强化技术噪音过滤

建议加入：

```text
软件开发、Bug 修复、日志分析、测试过程、命令输出和工具调用默认属于临时上下文。

除非内容明确属于长期架构决策、稳定工作方式、未来反复使用的项目事实，或用户明确要求保存，否则不要提取。
```

## 10.4 防止“详细度偏差”

建议加入：

```text
不要因为某个主题讨论时间长、描述详细或重复出现，就自动认为它值得长期保存。

记忆价值由未来用途和稳定性决定，不由文本长度决定。
```

## 10.5 保护少量重要信息

建议加入：

```text
即使生活事件、关系变化或长期偏好只出现一次，也要独立判断其长期价值，不要让大量技术内容掩盖它。
```

## 10.6 区分项目决策和开发过程

建议加入：

```text
项目长期决策可以提取，但具体修复过程、临时实现步骤和已完成的小改动不要提取。
```

---

# 十一、去重、合并和覆盖

## 11.1 与现有三级语义去重配合

提取侧应减少明显重复，入库侧继续负责最终防重。

需要支持：

- `duplicate`：完全或高度语义重复；
- `merge`：同一事实得到补充；
- `update`：已有事实内容发生变化；
- `supersede`：新事实取代旧事实；
- `discard`：无长期价值。

## 11.2 示例

旧记忆：

```text
用户计划继续使用 Operit 官方记忆库。
```

新信息：

```text
用户决定关闭官方记忆库，并由 CME 作为主要长期记忆源。
```

不应新增两个并存事实，应使用新信息覆盖旧信息。

## 11.3 去重范围

去重时结合：

- 语义相似度；
- 记忆类别；
- 相关实体；
- 角色或 Profile；
- 时间；
- 是否为更新关系；
- 是否为否定或修正；
- 是否属于同一事件。

---

# 十二、提取调度、节流和失败重试

## 12.1 当前策略

- 20 分钟冷却；
- 换对话或换角色立即分析。

## 12.2 优化方向

- 优先分析新增消息；
- 避免重复发送相同历史；
- 对超时进行有限次数重试；
- 重试时可缩小窗口；
- 记录失败范围，避免重复跳过；
- JSON 解析失败可进行一次格式修复；
- 分段中某一段失败时，不应导致全部结果丢失；
- 同一会话短时间大量更新时，合并分析任务。
- 分析完成回报：分析完成后 UI 及时显示结果（与水位线同源问题：CMS 12.2 分析完成但 UI 不刷新；CME 水位线丢失导致反复全量），v2.3.1 一并处理。

## 12.3 重试分级

建议区分：

- 网络错误；
- API 限流；
- 超时；
- 模型输出截断；
- JSON 格式错误；
- 空结果；
- 无长期记忆。

其中“空结果”和“无长期记忆”不能当作失败重试。

---

# 十三、测试与评估

## 13.1 回归测试场景

| 场景 | 预期结果 |
|---|---|
| 纯 Bug 调试 | 0 条长期记忆 |
| 长时间排查日志 | 0 条长期记忆 |
| 明确架构决策 | 提取 1 条项目长期记忆 |
| 用户明确表达新偏好 | 提取偏好 |
| 已有偏好被重复提到 | 不新增 |
| 用户修改旧偏好 | 更新或覆盖旧记忆 |
| 大量技术内容中夹杂生活事件 | 提取生活事件，忽略技术噪音 |
| 用户明确要求记住技术决策 | 提取 |
| 简短无效对话 | 全空数组 |
| 长对话但无长期信息 | 全空数组 |
| 跨窗口重复提到同一事件 | 合并成 1 条 |
| 模型无法确定事实 | 不提取 |
| 角色关系仅有暗示 | 不推断 |
| 明确共同经历 | 提取 relationship |
| 已完成的一次性任务 | 不生成 todo |
| 明确未来待办 | 生成 todo |

## 13.2 评估指标

不应只统计“提取了多少条”，建议统计：

- 精确率；
- 错误记忆率；
- 噪音条目比例；
- 生活事件召回率；
- 重复条目比例；
- 空结果正确率；
- 长会话成功率；
- API 超时率；
- JSON 解析成功率；
- 单次分析耗时；
- 每次分析平均输入 token；
- 每条最终记忆的成本。

长期记忆系统应优先优化精确率，而不是单纯提高召回率。

---

# 十四、是否参考官方自动和手动保存记忆

## 14.1 参考价值

官方已经实现：

- Agent 对话中主动保存记忆；
- 自动分析提取记忆；
- 手动分析提取记忆；
- 关闭角色读写官方记忆库；
- `user.md` 注入；
- 结构化更新 `user.md`。

这些能力可作为 CME 的对照实现。

## 14.2 重点研究内容

- 自动提取的调用时机；
- 自动提取的输入范围；
- 手动提取和自动提取的差异；
- 自动提取为何采用严格门槛；
- Agent 主动保存由什么触发；
- 官方记忆工具的描述；
- 是否存在独立 Memory Evaluator；
- 自动更新 `user.md` 是否依赖模型工具调用；
- 是否存在系统级事件钩子；
- 保存失败的重试策略；
- 去重和覆盖方式。

## 14.3 不直接照搬的部分

官方自动提取较严格，许多 Agent 能主动保存的内容会被自动提取判定为无价值。

这说明官方自动提取更适合作为：

- 低频；
- 高精确率；
- 防遗漏；
- 兜底机制。

CME 可参考这一原则，但不必完全复制官方的记忆类型和存储结构。

目标应是：

> 在保持谨慎准入的前提下，提供比官方更可控的分类、检索、隔离和扩展能力。

---

# 十五、实施优先级

## P0：当前重点

### 分析链路稳定性

- 复现长会话失败；
- 区分截断、超时、输出和质量问题；
- 增量分析；
- 分段或滑动窗口；
- 失败重试；
- 分段结果合并。

### 记忆质量

- 明确允许零结果；
- 不设置最低提取数量；
- 强化技术噪音过滤；
- 保护生活事件；
- 区分项目长期决策与临时开发过程；
- 改进跨窗口去重。

## P1：记忆准入与更新

- 候选记忆价值判断；
- `store / merge / update / supersede / discard`；
- 与已有数据进行更精确比对；
- 增加可观测性和拒绝原因；
- 建立回归测试集。

## P2：自更新系统

- Agent 自动判断并写入 CME；
- 独立 Memory Evaluator；
- 自动更新工作区状态文件；
- 参考官方自动保存和 `user.md` 更新实现。

## P3：主动召回

- Agent 主动调用 CME 检索工具；
- 自动注入不足时二次查询；
- 更精细的记忆详情读取。

由于自动注入已经实现，该项优先级较低。

## P4：Skill 自更新

暂不实现。

等待官方提供安全、稳定的 Skill 更新工具后再评估。

---

# 十六、约束

- 不改后端整体架构；
- 现有 Worker 无明显性能瓶颈；
- 优先优化分析链路本身；
- 不为了提高提取数量牺牲准确性；
- 不将临时技术过程写入长期用户记忆；
- 自动写入机制成熟前，不扩大 Agent 自由修改范围；
- 所有自动更新能力应支持日志、校验和回滚；
- Profile、角色和工作区之间必须保持记忆隔离。

---

# 十七、最终目标

CME 不追求“记住更多”。

CME 的目标是：

> 只保存真正能够改变未来 Agent 行为、理解和决策的信息。

在此基础上，逐步形成：

```text
对话
  ↓
长期价值判断
  ↓
谨慎写入
  ↓
准确检索
  ↓
自动注入
  ↓
必要时主动召回
  ↓
稳定更新记忆与工作区
```

最终使 CME 从一个“对话摘要与检索插件”，发展为：

> 可控、可解释、可隔离、可持续更新的 Agent 长期记忆系统。
